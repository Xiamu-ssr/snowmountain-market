---
name: wind-alice
description: 通过 DSH 原生 wind_alice 工具调用万得 Alice 专业金融分析能力。密钥由宿主凭证服务持有。
---

# Wind Alice（DSH 原生工具）

插件运行时会注册本 Skill；这里保留同一份人类可读说明。只使用 `wind_alice`
工具，不要从 Bash 调用 Wind CLI，也不要接触 `WIND_API_KEY`。

